import { PageLayout } from '../../layouts/PageLayouts';
import { MENU_NAVIGATION } from '../../shared/router';

export const HomePage = () => {
  return <PageLayout navigation={MENU_NAVIGATION}>HOME</PageLayout>;
};
